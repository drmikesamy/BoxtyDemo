@namespace Boxty.ClientApp.Pages.Components.Grids
@using Boxty.ClientBase.Extensions
@using Boxty.SharedApp.DTOs.UserManagement
@using Boxty.ClientApp.Pages.Components.Forms
@using Boxty.ClientBase.Components

<CrudGrid T="SubjectNoteDto" ItemForm="@SubjectNoteDialogContent">
    <PropertyColumn T="SubjectNoteDto" TProperty="string" Property="x => x.Content" Title="Content" />
    <TemplateColumn T="SubjectNoteDto" Title="Created Date">
        <CellTemplate>
            @context.Item.CreatedDate.ToLocalDisplayString(emptyText: "Not Available")
        </CellTemplate>
    </TemplateColumn>
    <PropertyColumn T="SubjectNoteDto" TProperty="string" Property="x => x.CreatedBy" Title="Created By" />
</CrudGrid>

@code {
    private RenderFragment SubjectNoteDialogContent = @<SubjectNotesForm />;
}
